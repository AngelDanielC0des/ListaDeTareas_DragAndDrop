spring.application.name=tareas

# Que las excepciones de Spring MVC (405, 415, 404 de recurso...) respondan en formato ProblemDetail,
# para que el frontend tenga un unico formato de error que interpretar.
#
# En esta versión no hay ningún @ControllerAdvice propio, asÃ­ que de esas excepciones se encarga
# directamente el manejador de Spring Boot y los códigos salen correctos.
spring.mvc.problemdetails.enabled=true
